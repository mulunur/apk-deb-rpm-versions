{
  "name": "apk-deb-rpm-versions",
  "version": "0.0.1",
  "license": "Apache-2.0",
  "devDependencies": {
    "@vitest/coverage-istanbul": "^1.4.0",
    "prettier": "3.2.5",
    "typescript": "^5.0.0",
    "vitest": "1.4.0"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "fmt": "prettier . --write"
  },
  "files": [
    "dist/"
  ]
}
